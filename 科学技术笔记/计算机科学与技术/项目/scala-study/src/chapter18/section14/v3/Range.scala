package chapter18.section14.v3

/**
  * Created by zlq on 3/12/17.
  */
/*class Range(val now: Int, val high: Int) extends Iterable[Int, Buffer]{
  override def iterator() = new Iterator[Int] {
    private var i = 0

    override def hasNext: Boolean = {
      i < length
    }

    override def next() = {
      i += 1
      i - 1
    }

    def build[F]() = new Buffer[F]
  }
}*/
